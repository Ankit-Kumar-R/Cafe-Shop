// Run this script in the browser console or server to promote a user to admin
// Or use Cloud SQL Studio.
console.log("To promote a user to admin, connect to Cloud SQL and run:");
console.log("UPDATE users SET role = 'admin' WHERE email = 'YOUR_EMAIL@gmail.com';");
